rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Allow wide open access for development
    // Triggered deployment at: 2025-02-28T10:45:00Z
    match /{document=**} {
      allow read, write: if true;
    }

  }
}